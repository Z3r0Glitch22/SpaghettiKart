#include <stdio.h>

void engine_run(const char *path) {
    printf("engine_run called with path: %s\n", path);
}

void engine_frame() {
    // Stub frame logic
}

void engine_draw_frame() {
    // Stub render logic
}